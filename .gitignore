# Binaries
bin/
*.exe
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# ==================== Sensitive Data ====================
# 配置文件体系已废除: 整棵配置连同凭据存在 data/jingzhe.db 的 config_kv 表内。
# 下面几条是"有人又把密钥写回配置文件"的防线, 一律不提交 (不再有 example 白名单例外)
config/
.env
.env.*

# SQLite database — 现含凭据 (tushare token / LLM key / SMTP 授权码), 绝不提交
# 建议同时 chmod 600; 若经 Syncthing 同步, 注意 .stversions 会保留历史版本里的旧凭据
data/*.db
data/*.db-journal

# Syncthing sync metadata under data/ (versioning, ignore rules, folder marker)
data/.stignore
data/.stfolder/
data/.stversions/

# Log files
logs/
*.log

# Backtest reports
reports/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
tmp/
tmp_verify/
temp/
*.tmp

# SQLite temp files
*.db-shm
*.db-wal

# Trae editor
.trae-html-share-packages/
.qoder/

# Hermes guide (outdated)
hermes-agent-guide/

# Python cache
__pycache__/
*.pyc

# Signal dump
signals.json
/dataloader
/server
/captain
.gocache/
.gomodcache/
/optimizer

# 本机环境标记 (开发环境声明, 不推送)
agent.md
